// Programa que suma dos números y devuelve el resultado

#include <stdio.h>

int main() {
    float x, y, z;

    x = 1;
    y = 10.0;

    z = x + y;

    printf("El resultado de la suma es: %f\n", z);

    return 0;
}
